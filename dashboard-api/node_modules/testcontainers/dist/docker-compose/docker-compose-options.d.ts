import { Env } from "../docker/types";
export declare type DockerComposeOptions = {
    filePath: string;
    files: string | string[];
    projectName: string;
    commandOptions?: string[];
    env?: Env;
};
