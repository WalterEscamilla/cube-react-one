import { ContainerName, HealthCheckStatus, NetworkSettings } from "../../types";
import { Port } from "../../../port";
import Dockerode from "dockerode";
export declare type InspectResult = {
    name: ContainerName;
    internalPorts: Port[];
    hostPorts: Port[];
    healthCheckStatus: HealthCheckStatus;
    networkSettings: {
        [networkName: string]: NetworkSettings;
    };
    state: {
        status: string;
        running: boolean;
    };
};
export declare const inspectContainer: (container: Dockerode.Container) => Promise<InspectResult>;
