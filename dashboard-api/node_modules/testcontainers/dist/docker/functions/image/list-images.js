"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.listImages = void 0;
const docker_image_name_1 = require("../../../docker-image-name");
const dockerode_1 = require("../../dockerode");
const logger_1 = require("../../../logger");
const listImages = () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const images = yield dockerode_1.dockerode.listImages();
        return images.reduce((dockerImageNames, image) => {
            if (isDanglingImage(image)) {
                return dockerImageNames;
            }
            const dockerImageNamesForImage = image.RepoTags.map((imageRepoTag) => docker_image_name_1.DockerImageName.fromString(imageRepoTag));
            return [...dockerImageNames, ...dockerImageNamesForImage];
        }, []);
    }
    catch (err) {
        logger_1.log.error(`Failed to list images: ${err}`);
        throw err;
    }
});
exports.listImages = listImages;
const isDanglingImage = (image) => image.RepoTags === null;
